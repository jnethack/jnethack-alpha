# AGENTS.md

## 概要
このリポジトリは、ゲームNetHackの日本語版であるJNetHackを扱います。

翻訳に便利なように原文と訳文の両方をソースに残していますが、このソフトウェアは日本語専用であり、英語を表示したり他の言語に切り替えたりはしません。

## ビルド方法

 - cd src
 - nmake package

## コーディングスタイル
### *.c
(先頭に追加する著作権表示にはこのルールを適用しません。)

英語版からコードを追加する場合は、次のスタイルで追加します。

```c
#if 1 /*JP*/
(追加するコード)
#endif
```

このif-endifはコードが追加されたことを示すためのもので、条件を変更することはありません。

英語版のコードを変更する場合は、次のスタイルで変更します。

```c
#if 0 /*JP*/
(オリジナル)
#else
(変更するコード)
#endif
```

このif-else-endifはどのようにコードを変更したかを示すためのもので、条件を変更することはありません。

単にオリジナルからコードを削除する場合は、else節を省略します。

`/*JP*/`の代わりに`/*JP:T*/`が使われている場合もあります。

英語版と変更するコードの両方が1行だけの場合、次のスタイルが使われている場合もあります。

```c
/*JP
(オリジナル)
*/
(変更するコード)
```
