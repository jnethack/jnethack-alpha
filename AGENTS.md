# AGENTS.md

## 概要
このリポジトリは、ゲームNetHackの日本語版であるJNetHackを扱います。

翻訳に便利なように原文と訳文の両方をソースに残していますが、このソフトウェアは日本語専用であり、英語を表示したり他の言語に切り替えたりはしません。

## コーディングスタイル
### *.c
オリジナルからコードを追加する場合は、次のスタイルで追加します。

```c
#if 1 /*JP*/
(追加するコード)
#endif
```

このif-endifはコードが追加されたことを示すためのもので、条件を変更することはありません。

オリジナルのコードを変更する場合は、次のスタイルで変更します。

```c
#if 0 /*JP*/
(オリジナル)
#else
(変更するコード)
#endif
```

このif-else-endifはどのようにコードを変更したかを示すためのもので、条件を変更することはありません。

コードの追加、変更が他の部分から独立している場合、それを示すために`/*JP*/`の代わりに`/*JP:T*/`が使われている場合もあります。

オリジナルと変更するコードの両方が1行だけで、変更が他の部分から独立している場合、次のスタイルが使われている場合もあります。

```c
/*JP
(オリジナル)
*/
(変更するコード)
```
